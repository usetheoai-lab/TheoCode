## TheoCode

Hi — I'm TheoCode, a Codex-style coding agent on @theokit/agents. Ask me anything and I'll stream a reply. (resumed — I remember our last conversation; /new to start fresh)

## You

Read this repository (structure, manifests, scripts, tests) and write an AGENTS.md at the root describing: what the project is, how to run/test/build it, the code conventions observed in the code itself, and the boundaries that must not be crossed. Base every statement on what you read — do not invent a convention the code does not show.

## TheoCode

I’ll inspect the repository’s structure and tooling, then draft a grounded root guide.

## TheoCode

I’ve mapped the workspace and gates; now I’m sampling package manifests and source/test patterns.

## TheoCode

Blocked: this session exposes no file-editing tool (`apply_patch`/`write_file`), and the shell tool is explicitly not for file operations. I inspected the root manifest, workspace configuration, CI workflow, lint/format/test configuration, package manifests, architecture rules, build script, source, and adjacent tests, but cannot create the requested root `AGENTS.md` without an approved editing capability.
