# O Prettier 3 já respeita o `.gitignore` por padrão — todo o estado de runtime (`.theokit/`, `dist/`)
# fica de fora sem precisar ser repetido aqui. Este arquivo declara APENAS o que é source versionado e
# ainda assim não pode ser reescrito por máquina.

# #321 — o CHANGELOG é histórico imutável: a regra de changelog do projeto proíbe editar entrada de
# versão já released, e `prettier --write .` mudava 224 linhas dele (inserção de linha em branco entre
# itens), quase todas em versões publicadas. Cosmético, silencioso e direto no registro de auditoria.
# A seção [Unreleased] é editada à mão, como as outras — formatador nenhum precisa entrar aqui.
CHANGELOG.md

# `.claude/` é o ecossistema CYCLE instalado a partir de `/home/paulo/Projetos/cycle` — código
# vendorizado, não fonte deste repositório. Ele tem o próprio repo, o próprio formatador e os
# próprios testes (`check_xrefs.py`, `test_e2e_smoke.py`). Reformatar aqui produziria 153 arquivos
# de diff cosmético que a próxima reinstalação desfaz, e o diff mascararia uma mudança real de
# contrato. O único conteúdo nosso lá dentro é `knowledge-base/`, que é registro de auditoria —
# mesma razão do CHANGELOG: histórico não se reescreve por máquina.
.claude/
